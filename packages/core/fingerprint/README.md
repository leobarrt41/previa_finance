# Fingerprint hardening

Goals:
- Add additional tests for `packages/core/src/fingerprint.ts` covering edgecases (accents, punctuation, abbreviations).
- Provide fuzzy matching helper and collision tests.

This folder contains notes and TODOs for the fingerprint-hardening effort.
